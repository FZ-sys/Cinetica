import styles from '../../styles/dashboard.module.css';

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Dashboard Cinetica</h1>
    </header>
  );
};

export default Header;
